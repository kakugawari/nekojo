# にゃん将 〜猫じゃらしで出世するにゃ〜

(リポジトリ名は nekojo のまま)

村を出た2頭身の子猫が、猫侍に仕えて合戦で手柄を立て、出世していき、
仲間を増やし、やがて城主になって城と村を大きくしていく。
「信長の野望」のこどもバージョンのつもり。**じゃらして、たたいて、のし上がる!**

作り方の決めごとやチェックリストは `app_template` リポジトリの CLAUDE.md
(このプロジェクトの雛形) に従う。ここにはこのアプリ固有のことだけ書く。

主人公の見た目・性格・戦い方は、もらった設定資料 (`art/character-sheet.png`) が正。

---

## 遊びの流れ

0. **タイトル** — 開くと毎回出る。絵に描いてある「はじめる」の札を押すと始まる。
   はじめて遊ぶときだけ、主人公の物語 (設定資料の文) が出る。
1. **合戦** — 「出陣!」で戦が始まる。敵は右から歩いてきて、近い順に奥へ斜めに並ぶ。
   殴ってくるのは先頭の 1 匹だけ (こども向け)。
   - **猫じゃらし**: 先頭の敵を引き寄せて夢中にする (その間は攻撃してこない)。
     敵が「!」と振りかぶった瞬間に当てると「見切り!」で、長く夢中になりスペシャルも多くたまる
   - **猫パンチ**: 届く敵をたたく。夢中の敵には会心 (2倍)。続けて当てるとコンボ。届かないと空振り
   - **スペシャル (必殺じゃらし)**: ゲージが満タンになったら、出ている敵ぜんぶに猫じゃらしコンボ
   - 最後は大将 (赤毛の猫)。勝つと手柄と資材、倒した相手が仲間になりたがることもある。
     負けても何も減らない (「ひと休み…」→「もう一度」)
2. **出世** — 稼いだ手柄の合計で段位が上がる (村の子猫 → 草履取り → … → 大名)。
   見た目も「村の子猫 → 旅立ち → 猫侍に仕える → お城を築く」と成長する (設定資料の成長の過程)。
3. **家臣** — 「足軽」で解放。役目は 自主練 (手柄が増える) / 普請 (資材が増える) / 出陣 (2人まで、合戦でいっしょに戦う)。
4. **城** — 「城主」で解放。斜め上から見た 6x6 のマスに、お城・猫侍の屋敷・訓練場・武器屋・
   見張り台・厩舎・石垣などを建てる。お城でお城の完成。家臣が地図の上を歩く。
5. **村** — 「城主」で解放。同じく 6x6 のマスに、民家・農場・田んぼ・工房・商店・温泉・井戸・
   桜や庭などのかざりを建てる。村人猫は放っておいても増え、地図の上を歩き回る (16匹まで描く)。
   建物ごとの効果 (家臣の枠・パンチ・体力・手柄・資材・人口の増え方) は `core.js` の
   `BUILDINGS` と `townEffects` にまとめてある。建てた物は取り壊せる (元の値段の半分が戻る)。

ゲームを開いていない間も時間は進む (最大 8 時間ぶんまで。合戦は進まない)。

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # ロジックのテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 npm i -D playwright)
node tools/extract-sprites.js   # 設定資料から img/ の絵を切り出し直す (要 playwright)
```

## 構成

```
index.html / styles.css   画面 (タイトル・物語・合戦・家臣・城・村・出世の札)
core.js                   ロジック。DOM を触らない。出世・合戦・家臣・城・村の計算
app.js                    操作・描画。戦場は 2 枚のキャンバス (背景は一度だけ描く / 動くものは毎コマ)
core.test.js              ロジックのテスト (node --test)。合戦の手ごたえも自動で遊ばせて見張る
browser-test.js           ブラウザで動かすテスト
serve.js                  開発用サーバー
title.jpg                 タイトルの絵 (元の PNG 2.2MB を JPEG 品質0.92 に焼き直して 426KB)
apple-touch-icon.png      アイコン (180x180。元の絵の透明な角を内側 80px で切り落とした)
img/                      切り出した絵。設定資料から (成長4段階・表情6つ・色違いの猫・必殺技のポーズ)、
                          施設の絵から (img/b-*.png。建物・かざり・村人猫の 29 点)
art/character-sheet.png   設定資料そのもの (切り出しの元)
art/building-sheet.png    施設の絵そのもの (切り出しの元。背景は透明で、建物のまわりに光のにじみ)
tools/extract-sprites.js  設定資料の切り出し。枠と消す所は設定資料の画素の位置で書いてある
tools/extract-buildings.js 施設の切り出し。にじみを透明度で落とし、つながった塊ごとに拾う
```

`window.__app` はテストから中身をのぞく・操作するための入口
(`state()` / `battle()` / `sortie()` / `punch()` / `lure()` / `setTab()` / `debugAddMerit()` など)。

## 決めごと

- **対象の端末は iPhone 16 Plus だけ。** 寸法は 430pt で決め打ち。
- **見た目は設定資料に合わせる。** クリームの紙・茶の墨・金の札・紺。ゲームとして絵づくりを
  決めているので、暗い画面用の配色は持たない。文字は丸ゴシック (iOS のヒラギノ丸ゴ)。
- **手柄 (`merit`) は「財布の中身」、出世は「稼いだ合計 (`totalMerit`)」で別に持つ。**
  家臣を雇ったり鍛えたりして手柄を使っても、出世は下がらない (下の落とし穴を参照)。
- **合戦の中身は `battle` という別の入れ物。** 毎コマ書き換える。終わったら
  `applyBattleResult` で state に反映する。合戦のタブを離れている間は止まる。
- **合戦の数字は、自動で遊ばせて決めた。** 「下手 (パンチ連打)」「ふつう (猫じゃらしも時々)」
  「上手 (振りかぶりを見て 0.15〜0.35 秒で猫じゃらし)」を段位ごとに 30 回ずつ戦わせて、
  下手は最初の数段だけ勝てる / ふつうと上手は全段位で勝てる、になるようにした。
  数字を変えたら `core.test.js` の「手ごたえ」の見張りを流す。
- **絵は設定資料から切り出す。** 地のクリーム色をふちからたどって抜く。後ろに絵が描き込まれて
  いる所 (お城を築く) は、右の輪郭を点でなぞって外を消している。
  **設定資料を差し替えたら、`tools/extract-sprites.js` の枠を測り直すこと。**
  絵の解像度は低い (ねこ 1 匹 120px ほど)。くっきりさせたいときは、透明な背景の大きな絵を
  1 枚ずつもらって `img/` の同じ名前で置き換えればよい (描く大きさは絵の高さから決まる)。
- **戦場を描く面の画素の倍率は 1.5 まで。** 絵の解像度が低いので 2 倍にしても細かくならない。
- **城と村の地図は、地面を一度だけ描き (map-bg)、建物と歩くねこは奥から順に毎コマ描く (map-fg)。**
  奥行きはマスの `gx + gy` で並べる。お城だけは 1 マスに置いて 2.2 マスぶんの大きさで描く。
  CPU4倍遅で建物18・村人猫16匹でも 16.7ms (60fps)。
- **建てる札は、資材が増えても作り直さない。** 押せるかどうかと値段だけを直す
  (押している最中に札が入れ替わると、押したことにならない)。
- **タイトルの「はじめる」は絵に描いてある。** 本物のボタンはその上に透明で重ねている
  (位置は元の絵の画素で測った値を % にしたもの。`styles.css` の `.title-start`)。
  **絵を差し替えたら、札の位置を測り直すこと。** 見張りは、重ねたボタンの位置を元の絵の画素に
  戻して、そこが札のクリーム色かを見ている (わざと 10% 上へずらすと落ちることを確かめた)。
- タイトルの絵は縦横比が画面より太い。幅 500〜560px で下端に合わせ、左右をはみ出させ、
  上に余る分は空の色で埋める。題字 (元の絵で x165〜915) が切れない範囲で決めている。
- ホーム画面の名前 (`apple-mobile-web-app-title`) やアイコンは、iOS がホーム画面に
  追加したときの設定を覚えている。**すでに追加済みなら、一度削除して追加し直さないと変わらない。**

## まだ手をつけていないこと

- 色違い (ノーマル・茶トラ・黒) の主人公は選べない。茶トラと黒は敵と家臣の姿に使っている。
- 効果音・音楽は無い。

---

## 踏んだ落とし穴 (このアプリ固有ぶん。共通のものは app_template 側の表を見る)

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 家臣を雇った直後、出世が下がって見え、雇ったはずの家臣が一覧から消えた | 「手柄」を出世の判定と、家臣を雇う代金の両方に同じ1つの数字で使っていた。雇うと数字が減り、出世の閾値を割り込むと、家臣タブがまた鍵つきに戻って中身ごと隠れた | **出世は「稼いだ合計 (`totalMerit`)」で判定し、使って減る「財布 (`merit`)」とは別に持つ。** 見張りは「家臣を雇っても `rankIndexOf` が下がらないこと」を見る (財布の中身だけを見る見張りだと気づけない) |
| パンチを連打するだけで、全段位を無傷で勝てた | パンチでのけぞった敵が殴れる距離の外に出るたび、攻撃の溜めを初めに戻していた。連打すると敵は永遠に攻撃できない | 先頭の敵は、一度前線に着いたら溜めを止めない。のけぞっても溜めは続き、戻った瞬間に殴る。見張りは「パンチ連打だけでは城代の戦に勝てない」 |
| 上手に遊ぶと、同じ敵を夢中にし続けて無傷で勝てた | 猫じゃらしの待ち時間 (2.6秒) が、夢中の長さ (3.4秒) より短かった | **待ち時間 > 夢中の長さ + 次の振りかぶりまで** にする (4.2秒 > 3.0 + 1.2)。自動で遊ばせる「上手」には、人の反応の遅れ (0.15〜0.35秒) を入れて測る |
| 最後の1匹をパンチで倒すと、勝利の札が出ずに止まった | 勝ちの知らせはパンチの中 (stepBattle の外) で出る。画面は知らせを「戦っている最中」にしか読んでいなかった | 知らせは、戦の中身がある限り毎コマ読む。見張りは「最後の1匹をパンチで倒しても勝利の札が出る」 |
| 戦場が縦長で、空ばかり広く、ねこが下に固まって主人公に敵が重なった | 横一列に並べていた。絵の幅 (120px) に対して戦場の横幅が足りない | 敵の列は**奥へ斜めに**並べる (奥ほど高く・小さく)。主人公と家臣は敵より後に (手前に) 描く。家臣が並ぶ場所として左に 17% 空ける |
| 遅い端末 (CPU4倍遅) の戦闘中に 30fps に落ちた | 戦場を画素の倍率 2 で描いていた。止めて測ると、文字・絵・光らせの二度描きのどれよりも、倍率 (塗る面積) が効いていた | 倍率を 1.5 にした (1コマ 33.3ms → 16.7ms、4秒で 154 → 207〜225コマ)。同じ文字を画像に取り置く案も試したが、測って差が出なかったので入れていない |
| 施設の絵を目分量の枠で切り出したら、29点中22点で枠のふちに絵が触れていた | 名札や隣の建物の旗が入り込んだり、逆に絵が切れたりしていた | 光のにじみを透明度で落とすと、建物・名札・煙がそれぞれ離れた塊になる。**塊ごとに番号を振り、枠は「中心がこの中にある塊を拾う」目印にだけ使う**。拾わなかった大きな塊を一覧に出して、名札の形 (横長・高さ30px前後) だけかを確かめる |
| 「お城」の名札だけ、拾わなかった塊の一覧に出てこなかった | 名札がお城の石段にくっついて、1つの塊になっていた (お城の高さが枠の 402px を超えて 435px) | その物だけ、決めた高さより下を切る (`CLIP_BOTTOM`)。名札の数 (29) と拾わなかった塊の数を突き合わせて気づいた |
